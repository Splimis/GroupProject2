<h1>Does this work?</h1>

<main>
  <div class="container">
    <div class="row">
      <div class="col s6">
      <ul>
 	      {{#each quote}}
  		    <li>
  	   	  <p>ID: {{this.id}}</p>
         	<p>{{this.quotation}}</p>
  		    </li>
  		  {{/each}}
      </ul>
      <button type="submit" class="btn btn-primary newquote" name="" id="button" data-id="{{this.id}}">
    
      <p>Random Quote</p>

      </button>
    </div>

    <h2>Add a Quote</h2>

<form class="create-form">

  <div class="form-group">
    <label for="char">Character:</label>
    <input type="text" id="char" name=Character>
  </div>

  <div class="form-group">
    <label for="quo">Quote:</label>
    <textarea id="quo" name="quote" rows="8" cols="40"></textarea>
  </div>

  <button type="submit">Add Quote</button>
</form>

  


{{{ sounds }}}


</html>
</main>